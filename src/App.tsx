import React, { FC } from 'react';
import Index from 'pages';
import { Container } from '@mui/system';

const App: FC = () => {
  return (
    <Container>
      <Index />
    </Container>
  );
};

export default App;
